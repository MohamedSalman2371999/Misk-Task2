import { CoutrySearchPipe } from './coutry-search.pipe';

describe('CoutrySearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CoutrySearchPipe();
    expect(pipe).toBeTruthy();
  });
});
